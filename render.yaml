services:
  - type: web
    name: kaktus-zagruzka
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
      - key: YTDLP_COOKIES
        sync: false
